/**
 * Outer height of QuickFilters chips — shared by rating circles and the save button
 * so interactive controls stay the same diameter as filter pills.
 */
export const CATALOG_CONTROL_SIZE_PX = 27;

/** Tailwind height class for filter chips (must stay a full literal for the scanner). */
export const catalogChipHeightClass = "h-[27px]";

/** Square size for circular rating / save controls matching chip height. */
export const catalogCircleControlClass = "h-[27px] w-[27px]";
